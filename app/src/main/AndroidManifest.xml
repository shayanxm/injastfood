<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shayanmoradi.injastfood">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dish"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.menu.MenuActivity">

        </activity>
        <activity android:name=".view.list.ShowListOfRestActivity" />
        <activity android:name=".view.restpage.RestPageActivity" />
        <activity android:name=".view.shoppingbag.ShoppingBagActivity" />
        <activity
            android:name=".view.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>